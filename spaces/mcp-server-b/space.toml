# MCP Server B space - defines a different MCP server for testing
# WHY: Tests MCP config composition from multiple spaces

schema = 1
id = "mcp-server-b"
version = "1.0.0"
description = "Space with MCP server B"

[plugin]
name = "mcp-server-b"
