# Multi-harness project for smoke testing
# WHY: Tests full multi-harness composition with different harness configurations

schema = 1

[targets.claude-dev]
description = "Claude-only development environment"
compose = ["space:path:../claude-only@dev"]

[targets.pi-dev]
description = "Pi-only development environment"
compose = ["space:path:../pi-only@dev"]
# harnesses = ["pi"]  # Phase 3: enable when harnesses support is added

[targets.full-stack]
description = "Full-stack multi-harness environment"
compose = ["space:path:../multi-harness@dev"]
# harnesses = ["claude", "pi"]  # Phase 3: enable when harnesses support is added
