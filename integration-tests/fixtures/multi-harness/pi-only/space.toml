# Pi-only space - uses extensions and skills
# WHY: Tests Pi-specific features like TypeScript extension bundling
schema = 1
id = "pi-only"
version = "1.0.0"
description = "Pi-specific development space with extensions and skills"

[plugin]
name = "pi-only"

[pi]
extensions = ["extensions/hello.ts"]

[pi.build]
bundle = true
format = "esm"
target = "bun"
