# Frontend space - React/frontend development workflows
# WHY: Provides commands and skills for frontend development

schema = 1
id = "frontend"
version = "1.1.0"
description = "Frontend development workflows"

[plugin]
name = "frontend"

[deps]
spaces = ["space:base@stable"]
