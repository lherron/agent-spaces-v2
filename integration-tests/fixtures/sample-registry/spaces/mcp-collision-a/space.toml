# MCP Collision A space - defines an MCP server that collides with another
# WHY: Tests MCP server name collision detection

schema = 1
id = "mcp-collision-a"
version = "1.0.0"
description = "Space with colliding MCP server (first)"

[plugin]
name = "mcp-collision-a"
