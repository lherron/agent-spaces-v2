{
  "name": "@agent-spaces/store",
  "version": "0.1.0",
  "description": "Content-addressed storage for Agent Spaces v2",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-spaces/core": "*",
    "@agent-spaces/git": "*",
    "@agent-spaces/resolver": "*"
  }
}
